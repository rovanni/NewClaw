[← Painel Executivo](../MASTER_EXECUTION_PLAN.md) · [Backlog](../ARCHITECTURAL_BACKLOG.md)

# Sprint 2026-10-S24 ⚠ Maior esforço do programa (empate com S25)
- **Número:** S24
- **Identificação Temporal:** 2026-10-S24 (executada em 2026-07-18)
- **Fase:** Execução Arquitetural
- **Epic:** Structural Simplification
- **Card ARCH:** ARCH-020 (+ ARCH-021, absorvido)
- **Objetivo:** Decompor `GoalExecutionLoop.runLoopInternal()` (~1030 linhas) e o `switch(cycleResult.outcome)` (~400 linhas, ARCH-021 absorvido aqui) em métodos/fases nomeadas.
- **Arquivos afetados:** `src/loop/GoalExecutionLoop.ts` (L570-1602).
- **Dependências:** S16 (ARCH-005), S03 (ARCH-006), S13 (ARCH-007), S17 (ARCH-008), S14 (ARCH-009). **Atualização (S14 adiada, 2026-07-17): não bloqueia mais — ver nota em S14; `REVISAO_CONSOLIDADA_TIPOS_PENDENTE.md`. Atualização (S17 adiada, 2026-07-18): idem — ver `docs/issues/009`.**
- **Checklist de execução:** padrão + mapear TODOS os efeitos colaterais capturados por closure antes de extrair qualquer método (mandatório — sem teste de sistema que cubra a função inteira hoje) + fatiamento em 2 incrementos sequenciais dentro da mesma Sprint (decisão do usuário — ver nota de estratégia abaixo).
- **Checklist de validação:** padrão + **Validação Progressiva completa até etapa 4 — obrigatória, executada nos 2 incrementos, não só no final.**
- **Rollback:** reverter os commits `0173795` (Incremento 1) e/ou `2782216` (Incremento 2), nessa ordem inversa.
- **Critérios de Aceite:** nenhum método resultante excede 300 linhas; comportamento observável idêntico. **Cumprido** — 11 métodos novos no total, maior é `runStepExecutionPhase` com 221 linhas; `runLoopInternal()` cai de ~1030 para 154.
- **Definition of Done:** unitário + regressão + e2e sintético + ambiente real. **Cumprido nos 2 incrementos.**
- **Commit esperado:** commits sequenciais por fase extraída. **Real:** `0173795` (Incremento 1 — switch), `2782216` (Incremento 2 — corpo pré-switch).
- **Status:** 🟢 Concluída.
- **Nota de estratégia (Fase 1, antes de implementar):** a Fase 1 (Compreensão) mapeou o método inteiro (587-1627 linhas na numeração pré-Sprint) e encontrou 2 blocos estruturalmente distintos — o switch (~350 linhas, 6 cases paralelos, mesmo padrão já resolvido em S22) e o corpo pré-switch `readyToValidate` (~394 linhas, árvore de decisão aninhada com seus próprios early-returns/continues, NÃO paralela). Decompor os dois num único commit concentraria o maior risco do programa sem checkpoint intermediário. Apresentadas 3 alternativas ao usuário (1 commit único / 2 incrementos sequenciais com validação própria cada / dividir em 2 Sprints formais); escolhida a de 2 incrementos sequenciais — reduz blast radius por commit sem violar WIP=1 (é uma Sprint, sequencial, nunca paralelo).
- **Achado real (premissa do card):** o tamanho citado (~1030 linhas) bateu quase exatamente com a medição real (confirmado por leitura completa do método) — diferente da maioria dos cards desta sessão, que precisaram correção de escopo. A única imprecisão real foi de CONTAGEM em documentos anteriores (ver nota "6 vs 8 pontos" em S23, categoria similar, não desta Sprint).
- **2 achados de bug reais durante a própria extração (mesma classe, 3 instâncias no total):** o discriminated-union pattern (`earlyReturn`/`continueLoop`/`priorFeedback`) exige que TODO branch que não reatribui `priorFeedback` explicitamente devolva o valor RECEBIDO como parâmetro, não um placeholder. `handleBlockedOutcome` (Incremento 1) e 2 branches de `runValidationNotAchievedPhase` (Incremento 2) inicialmente devolviam `priorFeedback` incorreto nessa situação — pego por leitura cuidadosa do código original linha a linha (grep de todo assignment a `priorFeedback` no arquivo), não por teste, antes de rodar qualquer validação. Nenhum dos 3 chegou a rodar contra a suíte com o bug presente.

