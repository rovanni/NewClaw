[← Painel Executivo](../MASTER_EXECUTION_PLAN.md) · [Backlog](../ARCHITECTURAL_BACKLOG.md)

# Sprint 2026-09-S22 ✅ Concluída
- **Número:** S22
- **Identificação Temporal:** 2026-09-S22
- **Fase:** Execução Arquitetural
- **Epic:** Structural Simplification
- **Card ARCH:** ARCH-022
- **Objetivo:** Decompor `GoalExecutionLoop.executeStep()` (~375 linhas) e eliminar os 4 blocos duplicados de "registrar falha" via helper `recordFailedAttempt()`.
- **Arquivos afetados:** `src/loop/GoalExecutionLoop.ts` (`executeStep` decomposto em orquestrador + `recordFailedAttempt`/`dispatchToolStep`/`dispatchAgentloopStep`/`finalizeStepAttempt`), teste novo `S122`, teste `S51` corrigido.
- **Dependências:** S16 (ARCH-005) — cumprida. S14 (ARCH-009), S21 (ARCH-013) — ambas adiadas, confirmado não-bloqueante (dependência era sequenciamento, não funcional).
- **Premissa reverificada (Fase 1) — parcialmente corrigida, sem gravidade suficiente para adiar:** comparação campo a campo dos "4 blocos quase idênticos" mostrou que só 3 são genuinamente equivalentes (step-name-as-path, catch, e o attempt-build do guard de auth). O guard de auth NUNCA chamava `evaluator.evaluate()` (retorna `needs_auth` direto) — diferente dos outros 3, que sempre chamavam. O 4º bloco ("fluxo principal") tem 5 campos extras (mutations/evaluation/traceId/subToolCalls/producedArtifactPaths) e não é exclusivamente um bloco de falha.
- **Decisão de design:** `recordFailedAttempt(goal, step, cycle, startMs, {output?, error?, toolName?, args?})` cobre só "construir attempt + persistir" (sem decidir o outcome) — cada chamador decide depois se chama `evaluator.evaluate()` ou retorna outra coisa, preservando a diferença real entre os 3 blocos. O 4º bloco manteve sua própria construção de attempt (mais rica) dentro de `finalizeStepAttempt()`, não forçado no helper.
- **Checklist de execução:** padrão — nenhuma dependência de ordem implícita nova encontrada (a ordem "guard de step-name-as-path antes do guard de auth antes da heurística de sucesso" já era assim e continua, só isolada em `dispatchAgentloopStep`).
- **Checklist de validação:** padrão + e2e sintético — `tsc --noEmit` limpo, `npm run build` limpo, regressão 125/125 (123 pré-existentes + `S122` novo com 21 assertions cobrindo os 4 cenários do card + 1 controle de caminho feliz; `S51` corrigido — asserção presa a `step.toolName` inline no source, mesma classe de S52/S110/S34/S11/S10). Ambiente real (etapa 4) não exigido pelo card (Risco: Médio, não Alto — método menor e mais localizado que ARCH-019/020).
- **Rollback:** reverter (mudança puramente estrutural, sem alteração de comportamento pretendida).
- **Critérios de Aceite:** nenhum bloco de "registrar falha" duplicado (3 de 4 unificados via helper, o 4º permanece separado por ser estruturalmente diferente, não por descuido); método principal com sub-métodos claros — **atingido**.
- **Definition of Done:** regressão 100% + e2e sintético — **atingido**.
- **Commit esperado:** 1 commit (implementação + testes); depois docs.
- **Status:** 🟢 Concluída em 2026-07-18.

