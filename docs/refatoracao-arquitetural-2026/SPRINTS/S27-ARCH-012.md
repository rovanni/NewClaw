[← Painel Executivo](../MASTER_EXECUTION_PLAN.md) · [Backlog](../ARCHITECTURAL_BACKLOG.md)

# Sprint 2026-11-S27 ✅ Concluída (2026-07-18) — RFC + implementação de escopo reformulado
- **Número:** S27
- **Identificação Temporal:** 2026-11-S27 (executada 2026-07-18)
- **Fase:** Execução Arquitetural
- **Epic:** Single Source of Truth
- **Card ARCH:** ARCH-012
- **Objetivo:** ~~RFC + implementação condicional para unificar `Goal.successCriteria` e `checkClaimsAgainstEvidence.CLAIM_RULES`~~ — **escopo reformulado pela própria RFC desta Sprint (Fase 1-4):** a investigação achou 3 mecanismos (não 2 — `structuralBypass` também decide "entrega comprovada"), premissa de duplicação só se sustenta para 1 de 5 `CLAIM_RULES`, e o problema real não era "estarem duplicados" — era 2 dos 3 mecanismos não terem a checagem de tipo de arquivo que o 3º já tinha (bug de 09/07 reaberto por 2 rotas não auditadas). Unificação de tipo rejeitada por risco; implementado o fechamento do gap real via predicado compartilhado.
- **Arquivos afetados:** `src/loop/planning/inferExpectedExtensions.ts` (`isExpectedDeliverableFile` nova), `src/loop/GoalExecutionLoop.ts` (`evaluateCriteria`, `structuralBypass` em `runValidationPhase`, `checkClaimsAgainstEvidence`).
- **Dependências:** S16 (ARCH-005, cumprida). ~~S18 (ARCH-018)~~ — dependência resolvida na própria Fase 1 desta Sprint: o escopo implementado não precisa de `ARCH-018` (ver nota no card `ARCH-012` do `ARCHITECTURAL_BACKLOG.md`) — **item de maior risco do programa inteiro, propositalmente o último.**
- **Checklist de execução:** padrão + documento de Fase 1-5 da `DIRETRIZ_ARQUITETURA_2026-07-13.md` completo ANTES de qualquer código. Cumprido — `RFC_ARCH-012_UnifiedDeliveryProof.md`.
- **Checklist de validação:** padrão + Validação Progressiva completa até etapa 4. Cumprido — tsc/build limpos, regressão 128/128 (127 + `S125` novo, 8/8 asserts), etapa 4 real (sandbox isolado, LLM real) confirmando que nem `structuralBypass` nem `evaluateCriteria` aceitam mais um arquivo de tipo errado como prova de entrega.
- **Rollback:** se a RFC concluir que o risco supera o benefício, encerra sem código — resultado válido e esperado, não uma falha do programa. Não exercido — RFC aprovou implementação de escopo reduzido.
- **Critérios de Aceite:** definidos na própria RFC. Atendidos.
- **Definition of Done:** RFC aprovada + (se implementado) Validação Progressiva completa. Atendida.
- **Commit esperado:** 1 commit (se implementado) ou 0 commits + documento de RFC (se não aprovado). Realizado: `0ca79ed`.
- **Status:** 🟢 Concluída — **último card executável do backlog de 26 itens.** Próximo passo do programa: Checkpoint de Encerramento `2026-12-CP05`.
- **Nota de posicionamento:** este card pertence ao Épico B tematicamente, mas sua execução foi deliberadamente adiada para o fim do programa — ver Painel Executivo para a posição cronológica real.

