[← Painel Executivo](../MASTER_EXECUTION_PLAN.md) · [Backlog](../ARCHITECTURAL_BACKLOG.md)

# Sprint 2026-10-S25 ⚠ Maior risco do programa
- **Número:** S25
- **Identificação Temporal:** 2026-10-S25 (executada em 2026-07-18)
- **Fase:** Execução Arquitetural
- **Epic:** Structural Simplification
- **Card ARCH:** ARCH-019
- **Objetivo:** Decompor `AgentLoop.runWithTools()` (~1793 linhas, o maior método do projeto) em fases nomeadas.
- **Arquivos afetados:** `src/loop/AgentLoop.ts` (L1118-2911).
- **Dependências:** S16 (ARCH-005), S22 (ARCH-022), S23 (ARCH-024-Impl). **NUNCA simultânea com S24.** Cumprida — S24 concluída antes de S25 começar.
- **Checklist de execução:** padrão + mapear TODOS os efeitos colaterais capturados por closure (`cycleHistory`, `usedToolInputs`, `stepCount`) antes de extrair qualquer método + fatiamento em 6 incrementos sequenciais em ordem crescente de risco (decisão do usuário — método ~1.7x maior que ARCH-020/S24, ~3x mais variáveis mutáveis por closure).
- **Checklist de validação:** padrão + **Validação Progressiva completa até etapa 4 — obrigatória, executada nos 6 incrementos.**
- **Rollback:** reverter os 6 commits na ordem inversa: `73d246c`, `0778076`, `45788e1`, `baf5d34`, `3b40632`, `53a4fa3`.
- **Critérios de Aceite:** nenhum método resultante excede 300 linhas; comportamento observável idêntico. **Parcialmente cumprido** — 8 dos 9 métodos novos ficam < 300 linhas; `runWithTools()` (o orquestrador remanescente) fica em 419 linhas, aceito explicitamente pelo usuário como exceção documentada (ver nota abaixo).
- **Definition of Done:** unitário + regressão completa + e2e sintético + ambiente real (fluxo completo de tool-calling com LLM real). Cumprido.
- **Commit esperado:** commits sequenciais por fase extraída. **Real:** `53a4fa3` (Inc.1), `3b40632` (Inc.2), `baf5d34` (Inc.3), `45788e1` (Inc.4), `0778076` (Inc.5), `73d246c` (Inc.6).
- **Status:** 🟢 Concluída.
- **Nota — por que `runWithTools()` ficou acima de 300 linhas:** Incremento 6 (setup) reduziu de 747 para 509; um 2º corte (context-growth guard, decisão do usuário via AskUserQuestion) reduziu para 419. O que resta é irredutível dentro da estratégia de "extrair fases": o esqueleto do `while` loop que orquestra as chamadas às fases já extraídas, as declarações que precisam ficar ANTES do `try{}` (achado real: `trace`/`fsm`/`move`/`turnAbort` não podem ser movidos para dentro de uma fase extraída chamada de DENTRO do try, porque se a criação de `trace` falhasse depois de movida, o `catch(fsmError)` do chamador leria `trace.status` de um `trace` nunca atribuído), e o `catch/finally` obrigatório (rede de segurança do lifecycle do trace). Fragmentar mais teria começado a quebrar o próprio esqueleto de controle do loop, não mais "extrair uma fase distinta" — usuário optou por aceitar como exceção documentada nesse ponto.
- **3 classes distintas de erro pegas por `tsc` antes de qualquer teste rodar, uma por incremento (4, 5, 6):** referência ausente (constantes esquecidas), tipo errado (dois tipos parecidos, campos diferentes), parâmetro não utilizado. Nenhuma é da mesma classe dos bugs de VALOR (threading de `priorFeedback`) achados em ARCH-020/S24 — os dois mecanismos de verificação (tsc para referência/estrutura, leitura cuidadosa para valor) são complementares, nenhum substitui o outro.

