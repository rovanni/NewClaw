/**
 * CapabilityProbe — Shim de compatibilidade.
 * Classe movida para CapabilityRegistry.ts (Fase 2.5) como implementação interna.
 * CapabilityProbe não é mais exportada — é usada apenas internamente pelo CapabilityRegistry.
 * @deprecated Este arquivo existe apenas para manter compatibilidade de imports legados.
 */

// CapabilityProbe não é mais exportada publicamente.
// Se você precisava instanciar CapabilityProbe diretamente,
// use CapabilityRegistry.getInstance() em vez disso.
export {};
